function lessfilter() {
  fs :tool lessfilter $@
}
function lz() {
  fs :tool liza $@
}

l()  { fs :tool lessfilter display  "${@:-.}"; }
la() { fs :tool lessfilter extended "${@:-.}"; }
ll() { fs :tool lessfilter info     "${@:-.}"; }
n()  {
  if (($#)) && [ -f "$1" ]; then
    fs :tool lessfilter edit "$@"
  else
    $EDITOR "$@"
  fi
}
o()  { fs :o "${@:-.}"; }

alias zf="fs :file --sort atime"

